def read1():
    шифровать = "
    с открытым(filename1, 'r', encoding='utf-8') как f:
        шифровать = шифровать.join(f)
    возвратное шифрование

def encryption(шифрование, ключ):
    зашифровано = ""
    алфавит = " abcdefghijklmnopqrstuvwxyz"
    для письма в шифровке.ниже():
        если буква в алфавите:
            позиция = алфавит.найти(письмо)
            newPosition = (позиция + клавиша) % len(алфавит)
            зашифрованный + = алфавит[новая позиция] 
        остальное:
            зашифрованное  письмо += 
    возврат зашифрован

def write1(зашифрованный):
	с открытым(filename2, 'w', encoding='utf-8') как f:
		f.запись(зашифрованная)

def read2():
	шифровать = "
	с открытым(filename2, 'r', encoding='utf-8') как f:
		шифровать = шифровать.join(f)
	возвратное шифрование

def decryption(шифрование, ключ):
	зашифрованный = ""
	алфавит = " abcdefghijklmnopqrstuvwxyz"
	для письма в шифровке.ниже():
		если буква в алфавите:
			позиция = алфавит.индекс(буква)
			newPosition = (position - key) % len(алфавит)
			encrypteed += алфавит[новая позиция]
		остальное:
			зашифрованная + = буква
	возврат зашифрован

def write2(зашифрованный):
    с открытым(filename3, 'w', encoding='utf8') как f:
        f.запись(зашифрованная)


filename1 = input('какой файл шифровать?: ')
filename2 = input('где расположить зашифрованный текст?: ')
filename3 = input('где расположить расшифрованный текст?: ')
key = int(input('ключ(1-25): ')) 

encrypt =  read1()
encrypt = шифрование(шифрование, ключ)
write1(шифрование)

encrypt = read2()
encrypt = расшифровка(шифрование, ключ)
write2(шифрование)
печать("готово")
